interface:
  display_name: "Salesforce Account Profile"
  short_description: "Build safe Salesforce account profiles"
  default_prompt: "Use $salesforce-account-profile to resolve an account safely and build a confirmed, confidential profile."
policy:
  allow_implicit_invocation: false
