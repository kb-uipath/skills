# Signal Implementation

This document defines the preserve-only boundary for BPMN signals.

## Preserve-only implementation

The model may preserve imported:

- Root `bpmn:signal` definitions.
- `bpmn:signalEventDefinition` references on start, intermediate, boundary, and end events.
- Signal throw/catch sequence flows and event subprocesses.
- Public-safe payload variable mappings.
- Diagram shapes and edges.

Do not generate new signal definitions, signal event definitions, signal
throws, signal catches, or signal-based event subprocesses in new source.

## CLI or operator-owned implementation

The CLI or operator must resolve:

- Any runtime subscription or cross-process signal binding outside standard BPMN XML.
- Payload schemas that are generated by a runtime resource.
- Tenant-specific channel, folder, or resource identifiers.
- Correlation, idempotency, and fan-out expectations when multiple deployed instances can catch the signal.
- Validation evidence that the deployed runtime supports the imported signal event shape.

## Validation expectations

- Imported signal event definitions still reference existing signal definitions.
- Unrelated edits preserve imported signal XML and diagram geometry.
- Any claim that an imported signal is executable has current runtime validation
  evidence outside this model-authored XML.
