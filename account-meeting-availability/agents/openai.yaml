interface:
  display_name: "Account Meeting Availability"
  short_description: "Manage contacts and rank availability"
  default_prompt: "Use $account-meeting-availability to manage account contacts or rank meeting slots from read-only Outlook free/busy evidence. Never create meetings or send messages."
